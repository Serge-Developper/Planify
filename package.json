{
  "name": "planify-vercel",
  "version": "1.0.0",
  "description": "Planify - Application de gestion d'emploi du temps et de gamification",
  "scripts": {
    "dev": "cd 'frontend (7)/frontend' && npm run dev",
    "build": "cd 'frontend (7)/frontend' && npm install && npm run build",
    "preview": "cd 'frontend (7)/frontend' && npm run preview",
    "install-deps": "cd 'frontend (7)/frontend' && npm install && cd ../../api && npm install",
    "vercel-build": "cd 'frontend (7)/frontend' && npm install && npm run build"
  },
  "keywords": ["planify", "emploi-du-temps", "gamification", "vercel"],
  "author": "Serge",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  }
}
