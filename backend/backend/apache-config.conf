# Configuration Apache pour Passenger Node.js
<VirtualHost *:80>
    ServerName api.planifymmi.fr
    
    DocumentRoot /var/www/vhosts/planifymmi.fr/api.planifymmi.fr/backend2
    
    <Directory /var/www/vhosts/planifymmi.fr/api.planifymmi.fr/backend2>
        AllowOverride None
        Require all granted
        
        # Configuration Passenger
        PassengerNodejs /opt/plesk/node/22.19.0/bin/node
        PassengerAppType node
        PassengerStartupFile app.js
        PassengerAppEnv production
        PassengerAppRoot /var/www/vhosts/planifymmi.fr/api.planifymmi.fr/backend2
    </Directory>
    
    # Headers CORS (liste blanche stricte)
    SetEnvIfNoCase Origin "^https?://(www\.)?planifymmi\.fr$" AccessControlAllowOrigin=$0
    Header always set Vary "Origin"
    Header always set Access-Control-Allow-Origin "%{AccessControlAllowOrigin}e" env=AccessControlAllowOrigin
    Header always set Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS"
    Header always set Access-Control-Allow-Headers "Content-Type, Authorization, X-Requested-With"
    Header always set Access-Control-Allow-Credentials "true"

    # Forcer 404 sur la racine pour ne pas exposer un message public
    <Location "/">
        SetHandler none
        RedirectMatch 404 ^/$
    </Location>
</VirtualHost>
  